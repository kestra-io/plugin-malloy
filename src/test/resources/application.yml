kestra:
  storage:
    type: local
    local:
      base-path: /tmp/unittest
  worker:
    controllers:
      type: STATIC
      static:
        endpoints:
          - host: localhost
  queue:
    type: memory
  repository:
    type: memory
